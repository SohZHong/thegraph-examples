{
  "name": "base-net",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ base-net",
    "create-local": "graph create --node http://localhost:8020/ base-net",
    "remove-local": "graph remove --node http://localhost:8020/ base-net",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 base-net",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.94.0",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
